import { Grid } from '@mui/material';
import React from 'react';

// interface InputEdited {}

const InputElement: React.FC = () => {
	return <Grid container></Grid>;
};

export default InputElement;
